# deps & build
node_modules/
coverage/
.nyc_output/
*.lcov

# generated dev PKI (regenerate with: npm run setup). Never commit private keys.
pki/

# Cloudflare / Wrangler (M2b+)
.wrangler/
.dev.vars
.deploy.env
dist/

# misc
*.srl
.DS_Store
*.log

# generated screenshots (regenerate with scripts/capture-*.mjs)
web/captures/
memory
patches/

# VICAL の生成物と retired アンカーは追跡する（秘密鍵ではなく、世代を辿るための記録）。
# interop のテスト資源だけは生成物なので除外。
interop/multipaz-jvm/src/test/resources/
.conformance-session
